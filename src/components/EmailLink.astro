---
interface Props {
  email: string;
  label?: string;
  class?: string;
}

const { email, label, class: className = '' } = Astro.props;
---

<a href={`mailto:${email}`} class={className} aria-label={label ?? email}>
  {label ?? email}
</a>
