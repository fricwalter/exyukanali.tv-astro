---
const channels = [
  {
    image: "/images/sports-category-compressed.webp",
    title: "Sportski Kanali",
    description: "Nogomet, Košarka, Borilački Sportovi i Više",
    gradient: "from-red-500 to-orange-500",
  },
  {
    image: "/images/movies-category-compressed.webp",
    title: "Filmovi i Serije",
    description: "10.000+ Naslova u 4K Rezoluciji",
    gradient: "from-purple-500 to-pink-500",
  },
  {
    image: "/images/nacionalni-tv-compressed.webp",
    title: "Nacionalni TV",
    description: "Svi kanali iz BiH, Srbije, Hrvatske, CG i MK",
    gradient: "from-blue-500 to-cyan-500",
  },
  {
    image: "/images/djeciji-program-compressed.webp",
    title: "Dječiji Program",
    description: "Crtani Filmovi, Animirani Sadržaj i Više",
    gradient: "from-green-500 to-emerald-500",
  },
  {
    image: "/images/dokumentarni-compressed.webp",
    title: "Dokumentarni",
    description: "Dokumentarci, Priroda, Nauka i Historija",
    gradient: "from-amber-500 to-yellow-500",
  },
  {
    image: "/images/internacionalni-compressed.webp",
    title: "Internacionalni",
    description: "Njemačka, Austrija, Švicarska, USA, UK i Ostali",
    gradient: "from-indigo-500 to-purple-500",
  },
];

---

<section id="channels" class="py-20 bg-stone-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-6">
        Sve Što <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-400">Želite Gledati</span>
      </h2>
      <p class="text-lg text-gray-300">
        Od domaćih vijesti do finala Lige Šampiona. Naša lista kanala pokriva sve interese i sve generacije.
      </p>
    </div>

    <!-- Channels Grid -->
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-8">
      {channels.map((channel, index) => (
        <div
          class="group relative overflow-hidden rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 hover:-translate-y-2"
          style={`animation-delay: ${index * 100}ms`}
        >
          <!-- Image -->
          <div class="relative h-56 overflow-hidden">
            <img
              src={channel.image}
              alt={channel.title}
              width={400}
              height={224}
              class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-500"
              loading="lazy"
            />
            <div class={`absolute inset-0 bg-gradient-to-t ${channel.gradient} opacity-80`}></div>

            <!-- Icon badge -->
            <div class="absolute top-4 right-4 w-12 h-12 bg-white/20 backdrop-blur-sm rounded-xl flex items-center justify-center">
              <span class="text-2xl">
                {index === 0 && "⚽"}
                {index === 1 && "🎬"}
                {index === 2 && "🇧🇦"}
                {index === 3 && "👶"}
                {index === 4 && "📚"}
                {index === 5 && "🌍"}
              </span>
            </div>
          </div>

          <!-- Content -->
          <div class="absolute bottom-0 left-0 right-0 p-6">
            <h3 class="text-xl font-bold text-white mb-2">{channel.title}</h3>
            <p class="text-white/80 text-sm">{channel.description}</p>
          </div>

          <!-- Hover overlay -->
          <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        </div>
      ))}
    </div>
  </div>
</section>
