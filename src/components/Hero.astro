---
const stats = [
  { label: "99.9%", value: "Uptime" },
  { label: "10.000+", value: "Kanala" },
  { label: "4K", value: "Kvalitet" },
  { label: "5000+", value: "Korisnika" },
];

const features = [
  { icon: "⚡", text: "Instant Aktivacija" },
  { icon: "🔒", text: "Sigurno Plaćanje" },
  { icon: "🌍", text: "EU Serveri" },
];
---

<section id="home" class="relative min-h-screen flex items-center pt-20 overflow-hidden">
  <!-- Video Background -->
  <div class="absolute inset-0 z-0">
    <video
      autoplay
      muted
      loop
      playsinline
      poster="/images/hero-bg.webp"
      class="w-full h-full object-cover"
    >
      <source src="/hero-video.mp4" type="video/mp4" />
    </video>
    <div class="absolute inset-0 bg-gradient-to-r from-blue-900/95 via-blue-900/80 to-blue-900/60"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-20">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Text Content -->
      <div class="text-center lg:text-left">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-white/10 backdrop-blur-sm rounded-full text-white/90 text-sm font-medium mb-6">
          <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
          🚀 #1 IPTV za Dijasporu
        </div>

        <h2 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6">
          Gledaj Najbolje <span class="text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-400">EXYU Kanale</span> Bilo Gdje u Svijetu
        </h2>

        <p class="text-lg sm:text-xl text-white/80 mb-8 max-w-xl mx-auto lg:mx-0">
          Bez zamrzavanja. Bez ugovora. <span class="font-semibold text-white">10.000+ kanala</span> i videoteka u 4K rezoluciji. Savršeno za naše ljude u Njemačkoj, Austriji i Švicarskoj.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          <a
            href="/narudzba/"
            class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-gradient-to-r from-yellow-500 to-orange-500 text-gray-900 font-bold rounded-xl shadow-lg shadow-orange-500/30 hover:shadow-xl hover:shadow-orange-500/40 hover:-translate-y-1 transition-all duration-200"
          >
            🎁 Testiraj 24h Besplatno
          </a>
          <a
            href="#pricing"
            class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-white/10 backdrop-blur-sm border-2 border-white/30 text-white font-semibold rounded-xl hover:bg-white/20 hover:border-white/50 transition-all duration-200"
          >
            Pogledaj Pakete
          </a>
        </div>

        <!-- Trust Badges -->
        <div class="mt-10 flex flex-wrap gap-6 justify-center lg:justify-start">
          {features.map((feature) => (
            <div class="flex items-center gap-2 text-white/90">
              <span class="text-xl">{feature.icon}</span>
              <span class="font-medium">{feature.text}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- Stats Card -->
      <div class="hidden lg:block">
        <div class="relative">
          <!-- Decorative elements -->
          <div class="absolute -top-4 -left-4 w-24 h-24 bg-gradient-to-br from-yellow-400 to-orange-500 rounded-2xl opacity-20"></div>
          <div class="absolute -bottom-4 -right-4 w-32 h-32 bg-gradient-to-br from-blue-400 to-purple-500 rounded-2xl opacity-20"></div>

          <div class="relative bg-white/10 backdrop-blur-md rounded-3xl p-8 border border-white/20">
            <div class="grid grid-cols-2 gap-6">
              {stats.map((stat, index) => (
                <div class={`text-center p-4 rounded-2xl bg-white/5 ${index === 0 ? 'bg-gradient-to-br from-yellow-500/20 to-orange-500/20' : ''}`}>
                  <div class="text-3xl sm:text-4xl font-bold text-white mb-1">{stat.label}</div>
                  <div class="text-white/70 text-sm">{stat.value}</div>
                </div>
              ))}
            </div>

            <div class="mt-6 p-4 bg-green-500/20 rounded-xl border border-green-500/30">
              <div class="flex items-center gap-3">
                <div class="w-3 h-3 bg-green-400 rounded-full animate-pulse"></div>
                <span class="text-white font-medium">Serveri locirani u Njemačkoj i Holandiji</span>
              </div>
            </div>

            <div class="mt-4 text-center">
              <p class="text-white/60 text-sm">Zadovoljnih klijenata širom svijeta</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Wave -->
  <div class="absolute bottom-0 left-0 right-0">
    <svg viewBox="0 0 1440 120" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full h-16 sm:h-24">
      <path d="M0 120L60 105C120 90 240 60 360 45C480 30 600 30 720 37.5C840 45 960 60 1080 67.5C1200 75 1320 75 1380 75L1440 75V120H1380C1320 120 1200 120 1080 120C960 120 840 120 720 120C600 120 480 120 360 120C240 120 120 120 60 120H0Z" fill="white"/>
    </svg>
  </div>
</section>
